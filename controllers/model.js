const Model = require ('ruta del Modelo')

const controller = {
    create: async(require, response) => {try{} catch{}},
    read: async() => {},
    update: async() => {},
    destroy: async() => {},
}

module.exports = controller